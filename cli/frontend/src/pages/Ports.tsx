export default function PortsPage() {
  return (
    <section className="page" aria-labelledby="page-title-ports">
      <div className="page-head">
        <h1 id="page-title-ports">Ports</h1>
      </div>
      <div className="card">
        <h2>No forwarded ports</h2>
        <p>Forwarded ports will show up here once a session is connected.</p>
      </div>
    </section>
  )
}
